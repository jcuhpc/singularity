# Common modulefile for SWNAME
# The following variable must be set
#     version:  an available version of SWNAME

if [ module-info mode load ] {
    puts stderr "
SWNAME v$version software is available in a singularity container.
To use SWNAME v$version, prepend the relevant, traditional command
used for application execution with:
	singularity run \$SING/SWNAME-$version.sif

Example:
	singularity run \$SING/SWNAME-$version.sif ...
"
}
