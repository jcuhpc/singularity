# Common modulefile for SWNAME
# The following variable must be set
#     version:  an available version of abyss

if [ module-info mode load ] {
    puts stderr "
SWNAME v$version software is available in a miniconda environment.
To enable use SWNAME v$version, execute the following commands:
	module load conda3
	source \$CONDA_PROF/conda.sh
	conda activate SWNAME-$version
"
}
